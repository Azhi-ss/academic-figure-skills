Flat vector academic network architecture diagram of the Whisper encoder-decoder Transformer on a pure white #FFFFFF canvas, 16:9. A convolutional audio encoder and a causal text decoder are bridged by cross-attention, with weight-tied output logits.

Two-column layout, equal-height columns with a central gutter carrying green cross-attention arrows. The left encoder sits on a very light #F0F4FF panel; the right decoder on a very light #FFF8F0 panel, so the two halves read as grouped. Left column, bottom to top: dark-grey "Log-Mel" block with a tiny mel-spectrogram thumbnail ("80 or 128 bins"); dashed-blue "Conv Frontend" ([Tune]) holding two thin rows "Conv1d k=3 + GELU" and "Conv1d k=3 s=2 + GELU" with a small stacked-feature-maps icon; light-grey dashed "Sinusoidal PE" ([Fixed], sinusoidal icon); the large "N × Encoder Block" container (2.5px #0072B2 border, stacked-layers-with-residual icon) holding LayerNorm (grey, bell-curve icon), Self-Attention (blue, 3×3 heatmap thumbnail, [Tune]), LayerNorm, MLP 4× (blue, fan-out-fan-in, [Tune]) with two curved residual arrows on the right; and a final LayerNorm. Right column, bottom to top: dark-grey "Token IDs" block showing a token-pill row ("SOT" black, rest grey); "Token Emb + PE" (orange, token-grid + sinusoidal icons, [Tune], "learned PE"); the large "N × Decoder Block" container (2.5px #E69F00 border) holding LayerNorm, Self-Attention (orange, heatmap, [Tune], "Causal mask"), LayerNorm, Cross-Attention (green #009E73, two-rows-dots-crossing icon, [Tune]), LayerNorm, MLP 4× (orange, [Tune]) with three curved residual arrows; a final LayerNorm; and a green "Logits" block ("Weight Tied"). Green #009E73 horizontal arrows cross the gutter from the encoder's final norm into every decoder Cross-Attention sub-layer. A dashed orange curved arrow loops from Logits back to Token Emb + PE, labeled "weight tying". Grey 9pt labels "(B,1500,80)", "(B,1500,D)", "(B,T,D)", "(B,T,V)" sit beneath their blocks.

Top-right margin at 65% scale: a borderless "Sizes" list—"tiny 39M / base 74M / small 244M / medium 769M / large 1550M / turbo 809M" in 10pt grey. Bottom legend: blue solid=encoder trainable, orange solid=decoder trainable, grey dashed=fixed [Fixed], green=cross-attention, orange dashed=weight tying. Conv kernel sizes, attention formulas, and layer counts deferred to caption.

Material: white fills, 1.5–2px colored borders, 4–6px corner radius, #4D4D4D forward arrows, monochrome line-art icons per block in its border color, no gradients/shadows/3D. Typography: Helvetica/Arial 12–14pt, titles 16pt semibold, dimensions 9pt grey. Aspect ratio 16:9.
