Flat vector academic network architecture diagram of a decoder-only Transformer (nanoGPT, GPT-2 style) on a pure white #FFFFFF canvas, 16:9. Token and position embeddings feed N repeated Pre-LN blocks to a weight-tied language model head.

Vertical bottom-to-top layout. The central column—about 55% of canvas width and 80% of height—is the visual anchor: a large rounded container with 2.5px dark navy #1B3A5C border titled "N × Block", resting on a very light #F0F4FF panel that also tints the embeddings and final norm. Inside, a tight vertical stack: LayerNorm (pale #8EAEC4, small bell-curve-over-bar icon), Causal Self-Attention (medium #2E6B9E, tiny 3×3 attention-heatmap-grid thumbnail, [Tune] pill), LayerNorm again, MLP 4× (medium #2E6B9E, fan-out-fan-in node icon, [Tune] pill). Two curved solid dark-grey residual arrows run along the container's right edge, bypassing the first two and last two sub-layers. Below the container: a thin Dropout bar, then a wider WTE+WPE block (light #5BA0D0) split into two columns—WTE with a token-grid matrix icon, WPE with a sinusoidal-waveform icon, both [Tune]—on the same #F0F4FF panel. Beneath it, a dark-grey-bordered Token IDs block showing a row of small rounded token pills: one black "CLS", several grey "SEP", others light grey. Above the container: Final LayerNorm, then LM Head (dark navy, output-node-to-class-labels icon, [Tune]). A dashed dark-navy curved arrow loops from LM Head back down to WTE along the far right, labeled "weight tying". Small grey 9pt dimension labels "(B,N,D)" sit beneath WTE+WPE and the N×Block, "(B,V)" beneath LM Head.

Right margin at 60% scale: a borderless "GPT-2 Sizes" column listing "124M / 350M / 774M / 1558M" in 10pt grey, each with a tiny stacked-layers thumbnail of increasing depth. A small legend bottom-left explains: solid border = trainable with blue [Tune] pill, dashed arrow = weight tying, solid curved arrow = residual. The Pre-LN formula and all hyperparameters (n_layer, n_head, n_embd, vocab) are not rendered—see caption.

Material: white fills, colored borders only; thin 1.5px outlines, 4–6px corner radius; arrows #4D4D4D; monochrome line-art icons in each block's border color; no gradients, shadows, or 3D. Typography: Helvetica/Arial 12–14pt labels, titles 16pt semibold, dimension notes 9pt grey. Aspect ratio 16:9.
