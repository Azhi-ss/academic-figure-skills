Flat vector academic module-relation diagram of a sparse non-ML Python scaffold (fixture-sparse: simulate.py, lennard_jones.py, integrator.py) on a pure white #FFFFFF canvas, 16:9. The central claim is evidence-insufficient connectivity: three code units that share no import-level connection.

Composition: one dominant block at top center, two smaller isolated blocks side by side at the bottom, and a central question-mark hub. The top "simulate.py" block is the visual anchor—2.5px #0072B2 border, about twice the visual weight of the bottom blocks—titled with a small terminal/CLI icon and an "[Entry Point]" pill. Inside, a vertical stack of two thin rows: "argparse --steps" and "print steps", with a solid dark-grey internal arrow between them; a small grey note "does NOT import src modules" sits beneath, on a very light #F0F4FF panel. Bottom-left: "lennard_jones.py" (orange #E69F00 solid border, small molecule/atom-graph icon, label "potential(r)", "pure function" tag, with a small grey contract label "V(r)=4[(1/r)^12−(1/r)^6]" beneath). Bottom-right: "integrator.py" (orange solid border, small stepped-arrow icon, label "step()", "Euler only" tag, faint note "not full Verlet"). These two sit on a very light #FFF8F0 panel. In the empty center between the three blocks, a large light-grey "?" with three DASHED light-grey #CCCCCC lines radiating outward to each module, each labeled "not connected in code" in 9pt grey; crucially, NO solid arrows connect any module to another. Top-right corner: a small green #009E73 dashed-border "Evidence Status" box ("Insufficient", "sparse repo") with a small magnifying-glass icon.

Supporting modules: a bottom-left legend explains solid dark-grey = actual in-file flow, dashed light-grey = evidence-insufficient/no code connection, blue = entry point, orange = scientific module, green dashed = evidence marker. Small grey signature labels under each module stand in for tensor-dimension notes (this repo has no learned arrays). A tiny side panel lists source files read: "simulate.py / lennard_jones.py / integrator.py / README.md" with check marks. The full LJ formula, the Verlet-vs-Euler discrepancy, and the missing force/state loop are deferred to caption, not rendered.

Visual hierarchy: simulate.py largest; bottom modules equal and smaller; evidence marker smallest. Material: white fills, 1.5–2px colored borders, 4–6px corner radius, monochrome line-art icons per block in its border color, no gradients/shadows/3D. Typography: Helvetica/Arial 12–14pt, titles 16pt semibold, notes 9pt grey. Aspect ratio 16:9.
