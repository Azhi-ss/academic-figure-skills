A flat vector sparse module-relation diagram for a small Python fixture project on a pure white #FFFFFF canvas, 16:9. Clean sans-serif font (Helvetica/Arial), 12–14 pt labels, dark grey #1A1A1A, centered in boxes. All boxes white fill with colored 2 px borders and rounded corners. No shadows, gradients, 3D, icons, emojis, or photographic elements. The central semantic is DISCONNECTION: the CLI is self-contained and has NO data flow to the two scientific modules; missing links are light-grey dashed lines with question marks, explicitly signaling evidence-insufficient rather than fabricated pipelines.

TOP center: "simulate.py [Entry Point]" box, blue #0072B2 2.5 px border, containing "argparse --steps" above "print steps" with a short internal solid #4D4D4D arrow between them, and a note "Does NOT import src modules". No solid arrows leave this box.

BOTTOM left: "lennard_jones.py / potential(r) / pure function" box, orange #E69F00 2 px border, with a small "LJ 12-6 (see caption)" note. BOTTOM right: "integrator.py / step() / Euler only" box, orange #E69F00 2 px border, with a "Not full Verlet" note. Both bottom boxes have NO incoming or outgoing solid arrows.

Across the open gap between top and bottom zones, draw a horizontal DASHED green #009E73 boundary line spanning most of the canvas width, with a centered dashed-border pill labeled "Evidence Insufficient". Below it, place a large light-grey #CCCCCC question mark "?". From it radiate THREE short DASHED #CCCCCC lines, each ending near (but NOT touching, and WITHOUT arrowheads) one of the three boxes, labeled "not connected in code": one up toward simulate.py, one down-left toward lennard_jones.py, one down-right toward integrator.py. There must be NO solid arrow between simulate.py and either bottom module, nor between the two bottom modules.

TOP-right corner: small "Evidence Status / Insufficient / sparse repo" badge with green #009E73 2 px DASHED border.

STYLE SPECIFICATIONS: Okabe-Ito — blue #0072B2 (entry), orange #E69F00 (modules), green #009E73 (evidence marker/boundary), light grey #CCCCCC (missing links), dark grey #4D4D4D (only the internal CLI arrow). The only solid arrow in the entire figure is the one inside simulate.py. Background #FFFFFF.

CAPTION NOTE: Source description, evidence level, LJ formula, integrator behavior analysis, and module-count details belong in the caption, not on the figure.