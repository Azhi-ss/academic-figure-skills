Flat vector academic network architecture diagram of a Neural Radiance Field (NeRF) MLP with positional encoding, a skip connection, and coarse-to-fine hierarchical sampling, on a pure white #FFFFFF canvas, 16:9.

Composition is a left-to-right flow with one dominant central stack and a smaller right-hand branch. At far left, two input blocks on a very light #F0F4FF panel: top, "xyz Position" with a small 3D-axes icon and label "Gamma(xyz) 63 dims"; bottom, "View Direction" with a small camera-frustum-with-rays icon and "Gamma(d) 27 dims". The xyz block feeds horizontally into the central 8-Layer MLP container—2.5px dark navy #1B3A5C border, white fill, titled "8-Layer MLP" with floating note "D=8 W=256", drawn largest at roughly 40% width. Inside, eight thin medium-blue #2E6B9E "Dense + ReLU" bars, each with a tiny fully-connected vertical-nodes icon; a curved solid dark-navy skip arrow runs along the left edge from the xyz input, bypassing layers 1–4 and merging into layer 5, labeled "skip". After layer 8 the stack splits: a light-blue "Alpha (1)" branch with a small opacity-circle icon, and a light-blue "Bottleneck" branch. The bottleneck joins the view encoding via a horizontal medium-blue arrow; a smaller "RGB Head" container (medium #2E6B9E) to the right shows one "Dense 128" bar then an "RGB (3)" block with a small image-frame icon. Small grey 9pt labels "(N,63)", "(N,256)", "(N,27)", "(N,3)" sit beneath the relevant blocks.

To the right, on a very light #FFF8F0 panel at about 65% scale, the sampling branch: "Coarse MLP" (dark navy, [Tune], MLP icon) feeds down to a dashed pale-blue "Sample PDF" block ("Inverse CDF", small branching-tree icon, "stop_grad" note), then down to "Fine MLP" (dark navy, [Tune]), then to a dark-grey "Volume Render" block ("Alpha Composite", dots-along-a-ray icon). A dashed grey arrow links coarse weights through Sample PDF to the fine MLP. Tiny legend bottom-left: solid = forward, dashed = sampling/stop-gradient, curved = skip; [Tune] = trainable. Positional-encoding equations, exact layer widths, and the volume-rendering integral are deferred to caption, not rendered.

Material: white fills, colored borders only, 1.5–2px outlines, 4–6px corner radius, #4D4D4D arrows, monochrome line-art icons in each block's border color, no gradients/shadows/3D. Typography: Helvetica/Arial 12–14pt, titles 16pt semibold, dimensions 9pt grey. Aspect ratio 16:9.
