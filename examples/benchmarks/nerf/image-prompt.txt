A flat vector academic architecture diagram of a NeRF MLP with coarse-to-fine branches, on a pure white #FFFFFF canvas, 16:9. Clean sans-serif font (Helvetica/Arial), 12–14 pt labels, dark grey #1A1A1A, centered in boxes. All boxes white fill with colored 2 px borders and rounded corners. No shadows, gradients, 3D, icons, emojis, or photographic elements. Three vertical zones: LEFT inputs, CENTER MLP/RGB head, RIGHT coarse-fine branches.

LEFT (top to bottom): "xyz Position / Gamma(xyz) / 63 dims" box, light blue #5BA0D0 border, with a solid dark grey #4D4D4D horizontal arrow RIGHT into MLP layer 1. Below it, "View Direction / Gamma(d) / 27 dims" box, #5BA0D0 border, with a solid medium blue #2E6B9E horizontal arrow RIGHT to the RGB head concat.

CENTER: tall "8-Layer MLP / D=8 W=256" container, dark navy #1B3A5C 2.5 px border. Eight thin stacked "Dense + ReLU" bars (#2E6B9E), four above and four below a skip join. A solid #1B3A5C curved arrow labeled "skip" originates from the xyz input, runs along the container's LEFT inner edge, bypasses layers 1–4, and merges into layer 5. Layer 8 output splits into "Alpha (1)" and "Bottleneck" boxes (#5BA0D0). To the right of the MLP top sits the "RGB Head / Dense 128" box (#2E6B9E), where the view-direction arrow enters with a "concat" label; inside it shows "Dense W/2 + ReLU" then "RGB (3)" (#5BA0D0).

RIGHT (top to bottom): "Coarse MLP [Tune]" box (#1B3A5C); solid arrow DOWN. "Sample PDF / Inverse CDF / stop_grad" box with pale blue #8EAEC4 2 px DASHED border; a DASHED dark grey #4D4D4D arrow passes through it from coarse to fine, representing importance sampling. "Fine MLP [Tune]" box (#1B3A5C); solid arrow DOWN to "Volume Render / Alpha Composite / RGB Map" box with dark grey #4D4D4D 1.5 px border.

STYLE SPECIFICATIONS: Nature Blue — #1B3A5C, #2E6B9E, #5BA0D0, #8EAEC4, #4D4D4D. Solid forward arrows #4D4D4D; skip arrow solid #1B3A5C curved; coarse-to-fine DASHED #4D4D4D; view concat solid #2E6B9E. Background #FFFFFF.

CAPTION NOTE: Source citation, positional-encoding formulas, sample_pdf mechanics, loss, and volume-rendering equations belong in the caption, not on the figure.